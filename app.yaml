command:
  - "bash"
  - "start.sh"

env:
  - name: DATABRICKS_NOTEBOOK_PATH
    description: "Caminho do notebook no workspace Databricks (ex: /Repos/user/project/notebook)"
  - name: DATABRICKS_CLUSTER_ID
    description: "ID do cluster existente no Databricks"
